# Clave de la API de Google Gemini, usada por el asistente de IA
# (src/services/geminiService.ts). Obtenla en https://aistudio.google.com/apikey
#
# Copia este archivo a `.env` y rellena el valor. `.env` está en .gitignore:
# nunca subas la clave al repositorio.
GEMINI_API_KEY=
